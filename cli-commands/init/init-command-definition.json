{
    "template": "init [with-example]",
    "description": "Build a development structure with deployment, contracts and tests folders inside",
    "options": [
        {
            "name": "with-example",
            "definition": {
                "describe": "Provides eosio.token contract and tests as an example for usage",
                "type": "boolean",
                "default": false
            }
        }
    ]
}